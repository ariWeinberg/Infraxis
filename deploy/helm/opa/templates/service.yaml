apiVersion: v1
kind: Service
metadata:
  name: cloudspace-opa
spec:
  selector: {app.kubernetes.io/name: {{ include "cloudspace-opa.name" . }}}
  ports: [{name: http, port: {{ .Values.service.port }}, targetPort: http}]
