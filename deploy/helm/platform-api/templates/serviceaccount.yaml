{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata: {name: {{ default (include "platform-api.name" .) .Values.serviceAccount.name }} }
automountServiceAccountToken: false
{{- end }}
