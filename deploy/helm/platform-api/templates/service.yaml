apiVersion: v1
kind: Service
metadata: {name: {{ include "platform-api.name" . }} }
spec:
  selector: {app.kubernetes.io/name: {{ include "platform-api.name" . }}}
  ports: [{name: http, port: {{ .Values.service.port }}, targetPort: http}]
